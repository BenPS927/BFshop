Small pure utility functions.
Avoid feature/domain logic here; keep business rules in dedicated domain modules.
utils is your toolbox. These are small helpers that make life easier, like formatting dates, formatting money, grouping arrays, things that are useful but not “business rules.”